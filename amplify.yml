version: 1
applications:
  - appRoot: slate
    backend:
      phases:
        preBuild:
          commands:
            - nvm install 22
            - nvm use 22
            - npm install --cache .npm --prefer-offline
        build:
          commands:
            - nvm use 22
            - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
    frontend:
      phases:
        build:
          commands:
            - nvm use 22
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - .npm/**/*
          - node_modules/**/*
